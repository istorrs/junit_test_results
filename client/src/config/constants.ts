/**
 * Global application constants
 *
 * IMPORTANT: These limits must match the backend constants.
 * DO NOT reduce these values without explicit user approval.
 */

// Maximum number of records to request from API
export const MAX_QUERY_LIMIT = 10000;

// Default limit for API requests
export const DEFAULT_QUERY_LIMIT = 10000;
